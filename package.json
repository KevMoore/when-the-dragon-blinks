{
  "name": "when-the-dragon-blinks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:site": "tsc && node scripts/build-site.mjs",
    "dev": "npm run build && npx http-server . -p 5173 -c-1",
    "serve:python": "npm run build && python3 -m http.server 5173"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "http-server": "^14.1.1"
  }
}
